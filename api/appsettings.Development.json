{
  "//RateLimits": "20 a minute is the production limit and it is right there. On a laptop it is not: a handful of mistyped sign-ins locks you out mid-session, and every developer shares one 'unknown' client IP. Development only - appsettings.json keeps the real number.",
  "RateLimits": {
    "AuthPerMinute": 200
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
