/// The number the app tells patients to ring when they cannot act themselves.
///
/// **Placeholder.** Nothing in the API publishes a switchboard number, so this
/// is hard-coded until one exists. It is here, alone, so replacing it is a
/// one-line change rather than a search through the screens.
abstract final class HospitalContact {
  static const reception = '+94 77 000 0000';
}
